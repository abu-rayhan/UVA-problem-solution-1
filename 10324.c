#include<stdio.h>

int main(){
    int i,j
    long long a[2];
    char s[1000009]
    while(scanf("%s",s)!=EOF){
        if(s[0]=='\0')break;
        for(i=0;s[i]!='\0';i++){
            if(s[i]=='0')z++;
            else o++;
            a[i]=z;
            b[i]=o;
        }
        scanf("%lld",&q);
        for(i=0;i<q;i++){
            scanf("%lld %lld",&x,&y);
            if(x>y){d=y;y=x;x=d;}
            a1=a[y-1]-a[x];
            a2=a[y]-
        }
    }
}
